[defaults]
# Silence the "discovered Python interpreter" notice (auto-discovery still works).
interpreter_python = auto_silent

# The ansible.posix.synchronize module emits internal deprecation warnings on
# newer ansible-core; they're not actionable from this playbook. Quiet them so
# deploy output stays readable.
deprecation_warnings = False
localhost_warnings = False
